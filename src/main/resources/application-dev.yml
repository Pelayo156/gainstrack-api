spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gainstrack
    username: admin
    password: admin
  jdbc:
    template:
      query-timeout: 10

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG